#pragma once

#include "Float2.h"

//---------------------------------------------------------------------------------
//	基本クラス
//---------------------------------------------------------------------------------
class Base
{
public:
	//	画像用の変数
	int		m_image;
	//	座標（ゲーム内座標）
	Float2	m_pos;

	Base();
	//	描画関数（どれだけずらして描画するかの値を渡して）
	void Render( float offset_x );
};
